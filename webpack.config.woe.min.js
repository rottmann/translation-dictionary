var webpack = require('webpack');
var pkg     = require('./package.json');

module.exports = {
    entry: './index.js',
    sourceMapFilename: 'translation-dictionary.woe.min.js.map',
    output: {
        path         : './build',
        filename     : 'translation-dictionary.woe.min.js',
        library      : 'TranslationDictionary',
        libraryTarget: 'umd'
    },
    devtool: 'source-map',
    plugins: [
        new webpack.DefinePlugin({
            WITHOUT_EVENTS: true
        }),
        new webpack.optimize.UglifyJsPlugin({
            minimize: true
        }),
        new webpack.BannerPlugin(pkg.name + ' v' + pkg.version + ' | ' + pkg.homepage + ' | ' + pkg.author + ' | ' +
                                 pkg.license)
    ]
};
